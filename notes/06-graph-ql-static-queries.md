with graphql we are just asking for what we want.

We need to name our queries so they are unique and not anonymous.

```
query SiteTitleQuery {
    ...
}
```

it will return stuff in an object which we can then parse.
